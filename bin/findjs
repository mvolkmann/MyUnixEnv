#!/bin/bash
# Finds all JavaScript files in and below the current directory
# that contain a given string.
# For example, findjs "prototype ="

if [ $# -ne 1 ]; then
  echo usage: findjs {search-string}
  exit 1
fi

find . -name '*.js' | xargs grep '$1'
